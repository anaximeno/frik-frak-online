import { Heading, VStack } from "@chakra-ui/react";

const RegisterPage = () => {
  return (
    <VStack paddingTop={100}>
      <Heading size="7xl">REGISTER</Heading>
    </VStack>
  );
};

export default RegisterPage;
